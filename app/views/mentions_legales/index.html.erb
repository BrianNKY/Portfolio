<p id="notice"><%= notice %></p>

<h1>Mentions Legales</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mentions_legales.each do |mentions_legale| %>
      <tr>
        <td><%= link_to 'Show', mentions_legale %></td>
        <td><%= link_to 'Edit', edit_mentions_legale_path(mentions_legale) %></td>
        <td><%= link_to 'Destroy', mentions_legale, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mentions Legale', new_mentions_legale_path %>
